<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit your chat</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="chat-container">
    <h2>Edit Chat</h2>
    <p class="chat-info">Chat ID: <span class="chat-id"><%= chat._id %></span></p>
    <p class="chat-info">From <strong><%= chat.from %></strong> to <strong><%= chat.to %></strong></p>

    <form class="chat-form" action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <textarea name="msg" rows="10"><%= chat.msg %></textarea>
      <button type="submit">Update Message</button>
    </form>
  </div>
</body>
</html>
